{
  "name": "hono-vite-pnpm-starter",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=18.17"
  },
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c auto \"pnpm -C apps/api dev\" \"pnpm -C apps/web dev\"",
    "build": "pnpm -C apps/api build && pnpm -C apps/web build",
    "start": "pnpm -C apps/api start",
    "typecheck": "pnpm -C apps/api typecheck && pnpm -C apps/web typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "typescript": "^5.6.2"
  }
}